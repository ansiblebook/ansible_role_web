---
# Invoke service state changes when desired.
- name: Control web server
  ansible.builtin.systemd:
    name: "{{ webserver }}"
    state: "{{ web_systemd_state }}"
    enabled: "{{ web_enabled_state }}"
    daemon_reload: true
